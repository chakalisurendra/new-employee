{
    "name": "new-employee-update-api-gateway",
    "version": "1.0.0",
    "description": "Employee update API using API Gateway and AWS Lambda",
    "main": "api.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "surendra",
    "license": "ISC",
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.415.0",
        "@aws-sdk/util-dynamodb": "^3.415.0",
        "aws-sdk": "^2.1460.0"
    },
    "devDependencies": {
        "serverless-iam-roles-per-function": "^3.2.0"
    }
}
